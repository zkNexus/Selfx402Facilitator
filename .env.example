# Server Configuration
PORT=3005
NODE_ENV=production

# Celo Mainnet Configuration (Required)
CELO_MAINNET_PRIVATE_KEY=your_mainnet_private_key_here
CELO_MAINNET_RPC_URL=https://forno.celo.org

# Supabase Database Configuration (Required for production)
# Get these from: https://app.supabase.com/project/_/settings/api
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Self Protocol Configuration (Required for QR verification)
# Use ngrok for local development: ngrok http --domain=your-domain.ngrok.io 3005
SELF_ENDPOINT=https://your-domain.ngrok.io/api/verify
SELF_SCOPE=self-x402-facilitator
SERVER_DOMAIN=https://your-domain.ngrok.io

# Notes:
# 1. CELO_MAINNET_PRIVATE_KEY: Private key with CELO for gas fees (keep secure!)
# 2. SUPABASE_URL: Your Supabase project URL (from project settings)
# 3. SUPABASE_SERVICE_ROLE_KEY: Service role key (has full database access)
#    WARNING: Keep this secret! Never commit to git or expose in client-side code
# 4. SELF_ENDPOINT: Public HTTPS endpoint for Self mobile app to verify proofs
# 5. SERVER_DOMAIN: Your public domain (use ngrok.io for local development)
# 6. If SUPABASE_* not set, facilitator runs in memory-only mode (nullifiers not persisted)
